# Project Status
status: active
summary: Implemented auto-triage orchestrator hook — migration 135 deployed, orchestrator deployed, WebUI toggle added. Pending Vercel prod deploy.
needs-human: Deploy WebUI to Vercel prod (`npx vercel --prod`), create a test idea with auto_triage=true to verify E2E flow
blocked-by: Vercel auth for prod deploy
sub-projects:
  - name: webui
    summary: Auto-triage toggle added to Ideas page header. Toggle reads/writes companies.auto_triage. Build passes.
    needs-human: Vercel prod deploy
    next-for-claude: Plan failed-job visual indicators on pipeline features (Chris's request)
  - name: orchestrator
    summary: autoTriageNewIdeas() implemented and deployed — 5-min cooldown, batch limit 3, uses request_standalone_work RPC. Runs as step 4b in tick loop.
    needs-human: Nothing — deployed
    next-for-claude: Monitor auto-triage logs after enabling
  - name: local-agent
    summary: Worktree freshness + cache-TTL Phase 1 implemented. Build fix shipped (PR #229) — removed spawnedAt ref from executor.ts.
    needs-human: Deploy migration 129, staging validation with Chris
    next-for-claude: Review Codex implementations, integration test plan
