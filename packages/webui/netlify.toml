# NOTE: This file is kept here for reference but the actual Netlify config
# lives at the repo root (../../netlify.toml) because the build requires
# workspace resolution from the monorepo root.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
