# Project Status
status: active
focus: true
summary: Pipeline infrastructure fully tested — all 8 tests pass (dispatch, DAG, parallel, contractors, workspace assembly, temp refs, slot routing, Entry Point A). Multiple bugs fixed (CardType validation, workspace permissions, report path, ARG_MAX, service role key).
needs-human: CPO persistent agent implementation (Chris WIP). Send Chris setup instructions for local agent.
next-for-claude: Report persistence (write full reports to DB). Remaining pipeline stages (combining, deploy-to-test, prod deploy). CPO stage skills.
blocked-by: CPO persistent agent (Chris WIP) blocks Entry Point A end-to-end
sub-projects:
  - name: idea-to-job-pipeline
    summary: All 8 pipeline infrastructure tests PASS. Contractors run with full workspaces, MCP tools, and skills. Only missing piece is CPO persistent agent as human-facing entry point.
    needs-human: Chris to implement persistent agent identity design
    next-for-claude: Report persistence to DB, orchestrator auto-chaining tests
  - name: personality
    summary: Exec personality system wired end-to-end — archetypes, compiler, trigger, seed personalities, orchestrator dispatch all shipped in PR #58
    needs-human: Deploy migrations 011 + 018, test live dispatch with personalityPrompt
    next-for-claude: Card 1.6 (local agent consumes personalityPrompt), 1.7 (CLI inspect/override)
  - name: knowledge
    summary: Knowledge Architecture v4 complete — 6 rounds of review. All 8 mandatory revisions applied.
    needs-human: Review final v4 document, approve for implementation
    next-for-claude: Phase 1 implementation (schema, RLS, prompt compiler, eval telemetry, seed data, A/B experiment)
  - name: triggers-and-events
    summary: V2.2 design doc complete. 9 subsystems + 3 cross-cutting capabilities (trust, estop, cheap-check). 3 rounds of review.
    needs-human: Review V2.2 doc, approve for cardify
    next-for-claude: /cardify to generate implementation cards
