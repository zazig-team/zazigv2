{
  "name": "@zazigv2/orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Supabase Edge Functions orchestrator — deterministic dispatch, no LLM",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@zazigv2/shared": "*"
  },
  "devDependencies": {
    "typescript": "5.8"
  }
}
