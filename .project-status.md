# Project Status
status: active
summary: SessionStart hook for persistent role prompt freshness shipped to master (b617658)
needs-human: Test check-prompt-freshness.sh live against a running persistent agent workspace; verify CLAUDE.md rewrite triggers correctly on prompt change
next-for-claude: Wire up hot-reload notification back to orchestrator (optional); expand hook to invalidate personality cache if personality_evolution_log changes
blocked-by:
sub-projects:
  - name: local-agent
    summary: SessionStart hook writes .role/.prompt-hash metadata and runs freshness check on each new Claude session
    needs-human: Live test with persistent CPO agent — confirm hook fires and CLAUDE.md updates on DB prompt change
    next-for-claude: Optional — event broadcast back to orchestrator on hot-reload; handle personality drift invalidation
  - name: personality
    summary: Phase 1 complete — schema, archetypes, prompt compiler, CLI, orchestrator wired
    needs-human: Deploy supabase migrations, verify live dispatch
    next-for-claude: Nothing until human verifies live
