# Project Status
status: active
summary: v3 iOS integration wired — migrations 232 (sidebar columns) + 233 (realtime + ideas SELECT RLS + widened source CHECK) ready to apply. Auto-triage and auto-spec already live via migrations 135/143/147/156/165/209/210/229. Integration contract at docs/plans/active/2026-04-15-v3-integration.md.
needs-human: Apply migrations 232 and 233 via Supabase Management API — SUPABASE_ACCESS_TOKEN not in Doppler (project `zazig` no longer exists; only `tom` and `trw-finances`). Register v3 magic-link redirect scheme in Supabase Auth URL allow-list. Run smoke-test curl against prod. Resend API key still pending.
next-for-claude: Wire v3 Swift side (separate repo ~/Documents/GitHub/zazigv3/). Resend email integration. CSO CLAUDE.md / operating manual. Accept proposal edge function.
blocked-by:
sub-projects:
  - name: webui
    summary: Proposal page fully redesigned with premium visuals — light/dark mode, DM Sans font, co-branded headers with separator, animated gate page, phase cards with gradient borders, Accept Proposal section + modal, refined typography and spacing.
    needs-human: Visual QA on proposal page after Vercel deploy
    next-for-claude: Accept proposal edge function, responsive testing on mobile devices
  - name: sales/live-beyond
    summary: First proposal seeded in DB with full content (8 sections, 2 phases, pricing)
    needs-human: Review proposal content, adjust pricing if needed, share URL with Marek
    next-for-claude: Client branding (logo, accent color) when provided
  - name: orchestrator
    summary: Auto-triage with atomic claims, stale session recovery, headless dispatch integrated into tick loop.
    needs-human: Nothing
    next-for-claude: Monitor auto-triage logs, tune batch sizes
  - name: local-agent
    summary: Expert session manager fixed — deduplication guard, prompt file piping, RLS update policy.
    needs-human: Nothing
    next-for-claude: Add telemetry for headless session duration tracking
