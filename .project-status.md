# Project Status
status: active
summary: Spring reconciliation complete. Pipeline clean (0 failed, 0 stuck). Dashboard polished. 3 manual builds shipped. 10 features in backlog awaiting spec.
needs-human: Tell Chris to resync skills (git pull && zazig skill sync && zazig stop && zazig start). Set Telegram Bot env vars. Prioritise which of the 10 unspecced features to tackle first.
next-for-claude: Spec highest-priority features from the 10 in backlog. Run standup sweep (1 new idea). Dedup overlapping features before speccing.
blocked-by:
sub-projects:
  - name: dashboard
    summary: Fully operational — pipeline board, ideas intake, detail panel, parked section, company selector all working
    needs-human: Nothing — dashboard is self-serve
    next-for-claude: Strategy tab (goals, focus areas) is a stretch goal
  - name: pipeline
    summary: Idle and clean. All reliability fixes shipped. Standalone dispatch operational.
    needs-human: Pick a feature to run through the pipeline to validate it end-to-end
    next-for-claude: Spec a simple feature and send through breakdown to test the full loop
