{
  "name": "@zazig/cli",
  "version": "0.78.0",
  "description": "Installable CLI for the zazigv2 local agent — login, join, start/stop/status",
  "type": "module",
  "bin": {
    "zazig": "./releases/zazig.mjs",
    "zazig-staging": "../../scripts/zazig-staging"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && chmod +x dist/index.js dist/staging-index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist tsconfig.tsbuildinfo tsconfig.build.tsbuildinfo"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "@zazigv2/local-agent": "*",
    "@zazigv2/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.24.0",
    "typescript": "5.8"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
