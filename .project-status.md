# Project Status
status: active
summary: Memory system design v3 complete — incorporated Codex second-opinion feedback. Now 9-type taxonomy (added Procedure), tier-specific memory budgets, mandatory slot reservation, hardened Context Handoff Protocol (4 failure modes + safeguards). Ready for Phase 1 implementation.
needs-human: Review memory design doc and approve. Decide on Phase 1 timeline. Answer open questions (existing memory_chunks data, extraction model choice, cross-company contractor memory timeline). Tell Chris to resync skills.
next-for-claude: Implement Phase 1 — memories table migration (deprecate memory_chunks), extract-memories Edge Function, memory_save and memory_search MCP tools, ContextPack assembler, post-job extraction hook in orchestrator, basic bulletin assembler for persistent agents.
blocked-by:
sub-projects:
  - name: dashboard
    summary: Fully operational — pipeline board, ideas intake, detail panel, parked section, company selector all working
    needs-human: Nothing — dashboard is self-serve
    next-for-claude: Strategy tab (goals, focus areas) is a stretch goal
  - name: pipeline
    summary: Idle and clean. All reliability fixes shipped. Standalone dispatch operational.
    needs-human: Pick a feature to run through the pipeline to validate it end-to-end
    next-for-claude: Spec a simple feature and send through breakdown to test the full loop
